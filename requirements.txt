# NOTE: install PyTorch separately (CPU vs CUDA vs MPS differs by platform).
# - RunPod/CUDA: install from PyTorch CUDA wheels index, then `pip install -r requirements.txt`.
# - Local/MPS: `pip install torch` (or use the official macOS install instructions).
transformers==5.5.3
tokenizers==0.22.2
huggingface-hub==1.10.1
safetensors==0.7.0
sentencepiece==0.2.1

numpy==2.4.2
scipy==1.17.1
scikit-learn==1.8.0

# For parquet I/O + Stage 2/3 analyses/figures
pandas==2.3.2
pyarrow==23.0.1

matplotlib==3.10.8

